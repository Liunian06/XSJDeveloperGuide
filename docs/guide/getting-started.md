# Getting Started

Once your environment is set up, you can start building.

## Building the Core
Run the build script to compile the shared C++ library.
```bash
./scripts/build_core.sh
```

## Running the Android App
Open `android/` in Android Studio and run `app`.

## Running the iOS App
Open `ios/` in Xcode and run `SmallPhone`.
