# Small Phone 使用教程

本文档将引导您构建第一个 Small Phone 应用程序。

1.  **新建项目**: 使用 CLI 工具 `sp create my-app`
2.  **编写 UI**: 在 `ui/` 目录下编写 Compose (Android) 或 SwiftUI (iOS) 代码。
3.  **连接逻辑**: 使用 FFI 或 JNI/ObjC++ 连接 C++ 核心。
4.  **调试运行**: `sp run android` 或 `sp run ios`。

详细步骤请参阅各平台子文档。
